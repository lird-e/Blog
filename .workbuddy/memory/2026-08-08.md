# 2026-08-08 工作日志

## 项目体检（全量代码审查）
- 对博客项目做了全量检查，产出报告：`.workbuddy/reports/blog-audit-2026-08-08.html`
- 发现高优先级问题 4 个：
  1. Decap CMS `public_folder: "/assets/uploads"` 绝对路径，子路径部署下插图必 404 → 应改 `/Blog/assets/uploads`
  2. publish.bat 依赖说明缺 pyyaml（build.py import yaml）→ 本地按提示装必报错；本机目前无 .venv
  3. serve.py 双重 URL 解码（parse_qs 后又 unquote）→ 正文含字面 %xx 被二次解码损坏（已实测）
  4. 文章按文件名 reverse 排序而非 frontmatter date，serve.py/Decap 产出的无日期文件名会排错位
- 中优先级：slug 冲突覆盖、中文 URL 不稳定、文章页 title 未转义（首页有转义）、serve.py 无认证绑 0.0.0.0、深色模式代码高亮失效、两套发文后台并存
- 低优先级：Giscus 占位符、缺 sitemap、markdown 放行内联 HTML 等
- 沙箱 pip 网络受限（pypi SSL 失败），未能实跑构建验证，结论基于静态审查 + 逻辑实测

## 高优先级修复执行（H1-H4，全部完成并本地验证）
- **H1** admin/config.yml `public_folder` → `/Blog/assets/uploads`（子路径部署防 404）
- **H2** 新建 `requirements.txt`（markdown/pygments/pyyaml）；publish.bat 改 `pip install -r requirements.txt` 并补 `python build.py`（public 被 gitignore，首次需先构建）；deploy.yml 统一 `pip install -r requirements.txt`
- **H3** serve.py 去掉双重 unquote（parse_qs 已解码），删除 unquote 导入；冒烟验证正文 `%E4%B8%AD` 原样保留
- **H4** build.py 按 frontmatter date 降序排序（`posts.sort(key=lambda p: (p["date"]=="", p["date"]), reverse=True)`），去掉文件名排序；验证新文章排最前
- **附带修复（冒烟测试新发现）** serve.py do_POST 的 302 Location 头中文 slug 导致 UnicodeEncodeError（latin-1）→ 改 `quote(slug)`；中文标题发布必现，已修并验证 302
- 本机已创建 `.venv` 并安装依赖（markdown 3.10.3/pygments 2.20.0/pyyaml 6.0.3），build.py 构建通过
- 坑：本机系统环境 HTTP(S)_PROXY=127.0.0.1:7897 代理未运行 + pip index-url 残缺（`https://pypi.tuna.tsing`）→ 安装依赖需 `HTTP_PROXY= HTTPS_PROXY= pip install -r requirements.txt --index-url https://pypi.tuna.tsinghua.edu.cn/simple`
- 待办：中/低优先级（M1-M6、L1-L5）未处理，见体检报告
