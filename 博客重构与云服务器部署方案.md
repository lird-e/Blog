# 博客重构与云服务器部署方案

> 项目: lird-e/Blog · 目标环境: Ubuntu 云服务器(IP 直连) · 生成日期: 2026-09-24

---

## 一、现状与目标

**现状**:
- 纯 Python 自研静态生成器(`build.py`)构建到 `public/`,零 Node 依赖
- 部署链路: `publish.bat`(commit + push)→ GitHub Actions → GitHub Pages
- 已有特性: Primer 主题(明暗双主题)、SEO(canonical/OG/Twitter Card)、RSS、sitemap、站内搜索、标签系统、Sveltia CMS 在线后台、Giscus 评论(基于 GitHub Discussions)
- 内容存量: 29 篇 Markdown 文章(`content/posts/`)

**目标(本次确认的需求)**:
1. 重构为**前后端分离**应用(前端 + 后端 + 数据库)
2. 自建**评论区**功能(替代 Giscus)
3. 部署到 **Ubuntu 云服务器**,通过 **IP 直接访问**
4. **停用 GitHub Pages**(服务器为唯一站点)

---

## 二、技术选型

| 层 | 选型 | 理由 |
|---|---|---|
| 后端 | **Go 1.22+ / Gin** | 与考勤系统重构技术栈统一,一套技能两个项目复用;编译为单二进制,部署简单、内存占用极低 |
| 数据库 | **SQLite**(modernc.org/sqlite,纯 Go 驱动) | 单文件零运维,博客量级绰绰有余;免 CGO 交叉编译友好;未来可平滑迁 MySQL/PostgreSQL |
| 前端 | **React 18 + Vite + React Router** | 与考勤系统一致;构建产物为纯静态文件,由 Nginx 直接托管 |
| Markdown 渲染 | **goldmark**(Go 端) | 入库时渲染一次存 HTML,与 `build.py` 思路一致,请求时零渲染开销 |
| 反向代理 | **Nginx** | 托管前端静态文件 + `/api` 反代 + gzip + 静态资源缓存 + 限流 |
| 进程管理 | **systemd** | 开机自启、崩溃自动重启 |
| 管理端认证 | **JWT + bcrypt** | 单管理员场景,简单够用 |
| 代码同步 | **GitHub(本地 push)+ Gitee 镜像(服务器 pull)** | 国内云服务器直连 GitHub 拉取经常超时,Gitee 镜像走国内线路稳定(你已有 Gitee 账号 manboman) |

> 设计原则: 与考勤系统的 Go(Gin) + React 技术栈完全统一,两个项目共用一套工程经验,简历叙事也更聚焦。

---

## 三、总体架构

```
浏览器(IP:80)
   │
   ▼
┌───────────────── 云服务器 (Ubuntu) ─────────────────┐
│  Nginx (:80)                                        │
│    ├── /            → React 构建产物 (dist/ 静态)    │
│    ├── /api/*       → Go(Gin) 服务 127.0.0.1:8080   │
│    │                    └── SQLite (/var/blog/blog.db)│
│    └── /assets/*    → 静态资源(强缓存 7 天)           │
└─────────────────────────────────────────────────────┘
   ▲
   │ ssh + deploy.sh(git pull → build → restart)
GitHub main(你 push)──镜像──▶ Gitee(服务器 pull)
```

- Go 服务**只监听 127.0.0.1:8080**,不直接对外,一切流量经 Nginx
- SQLite 数据库文件与代码目录分离,便于备份

---

## 四、功能与接口设计

### 4.1 公开接口(游客)

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/posts?page=&tag=&q=` | 文章列表(分页 / 标签筛选 / 搜索) |
| GET | `/api/posts/:slug` | 文章详情(HTML 内容 + 浏览量自增) |
| GET | `/api/tags` | 标签云 |
| GET | `/api/posts/:slug/comments` | 评论列表(楼中楼结构) |
| POST | `/api/posts/:slug/comments` | 发表评论(公开,有防垃圾限制) |
| GET | `/rss.xml`、`/sitemap.xml` | 后端动态生成(保留现有 SEO 能力) |

### 4.2 管理接口(JWT 保护,`/api/admin/*`)

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/api/admin/login` | 登录,返回 JWT(有效期短 + 刷新) |
| GET/POST/PUT/DELETE | `/api/admin/posts` | 文章 CRUD(Markdown 编辑,保存时 goldmark 渲染) |
| GET | `/api/admin/comments?status=` | 评论审核列表(待审/已过) |
| PUT/DELETE | `/api/admin/comments/:id` | 通过 / 拒绝 / 删除评论 |

### 4.3 评论功能细节

- 支持楼中楼(`parent_id` 回复)
- 游客填写: 昵称(必填)+ 邮箱(选填,仅存 hash 用于生成头像,不存明文)
- 默认**先发后显**(status=approved);后台可一键切换为**先审后显**(P2 防垃圾加强)
- 防垃圾四件套:
  1. 频率限制: 同 IP 每分钟 1 条、每日 20 条(Nginx limit_req + 后端计数双保险)
  2. 内容长度 1~2000 字符
  3. 蜜罐隐藏字段 + 提交时间 < 3 秒直接拒绝(挡脚本)
  4. IP 只存加盐 hash,不存明文(隐私安全)

---

## 五、数据库设计(SQLite)

```sql
CREATE TABLE posts (
  id           INTEGER PRIMARY KEY AUTOINCREMENT,
  slug         TEXT UNIQUE NOT NULL,        -- 沿用现有 slug,未来绑域名后旧链接可 301
  title        TEXT NOT NULL,
  excerpt      TEXT DEFAULT '',
  content_md   TEXT NOT NULL,               -- Markdown 原文(唯一事实来源)
  content_html TEXT NOT NULL,               -- goldmark 渲染结果
  tags         TEXT DEFAULT '',             -- 逗号分隔
  views        INTEGER DEFAULT 0,
  published    INTEGER DEFAULT 1,
  created_at   TEXT NOT NULL,
  updated_at   TEXT NOT NULL
);

CREATE TABLE comments (
  id         INTEGER PRIMARY KEY AUTOINCREMENT,
  post_id    INTEGER NOT NULL REFERENCES posts(id) ON DELETE CASCADE,
  parent_id  INTEGER REFERENCES comments(id),
  nickname   TEXT NOT NULL,
  email_hash TEXT DEFAULT '',
  content    TEXT NOT NULL,
  ip_hash    TEXT DEFAULT '',
  status     TEXT DEFAULT 'approved',       -- approved / pending / rejected
  created_at TEXT NOT NULL
);

CREATE INDEX idx_comments_post ON comments(post_id, status, created_at);
CREATE INDEX idx_posts_created ON posts(created_at DESC);
```

管理员账号不建表,**密码 bcrypt hash 存服务器环境变量**(单管理员,免维护)。

---

## 六、前端设计(React)

### 6.1 页面与路由

| 路由 | 页面 | 说明 |
|---|---|---|
| `/` | 首页文章列表 | 分页、标签筛选、搜索框 |
| `/post/:slug` | 文章详情 | 渲染 content_html + 评论区组件 |
| `/tags`、`/tag/:tag` | 标签页 | |
| `/search?q=` | 搜索结果页 | P0 先用 SQL LIKE,P2 升级 SQLite FTS5 |
| `/about` | 关于页 | |
| `/admin` | 管理后台 | 登录 + 文章编辑器 + 评论审核 |

### 6.2 样式策略

- 直接**复用现有 Primer 主题 CSS**(`assets/` 下的样式与明暗切换逻辑已验证),迁移成本最低
- CSS 版本号缓存策略(你之前修过的 CSS 缓存问题)继续沿用: 文件名 hash 由 Vite 构建自动完成
- 明暗主题跟随系统 + 手动切换的逻辑保持不变

---

## 七、数据迁移

### 7.1 文章迁移(P0,一次性脚本)

`server/migrate/main.go`:
1. 遍历 `content/posts/*.md` + `content/about.md`
2. 解析 frontmatter(title/date/tags/excerpt/slug)——与你 `build.py` 中现成的解析逻辑对照实现
3. goldmark 渲染 Markdown → HTML
4. 批量 INSERT 到 `posts` 表(slug、日期保持原值)

> `content/` 目录在仓库中**保留**,作为 Markdown 原文的备份与迁移源。

### 7.2 Giscus 旧评论(P2,可选)

Giscus 的评论存在你仓库的 GitHub Discussions 里。若需要保留: 写一个脚本通过 GraphQL API 导出 → 映射为 `comments` 表记录(昵称填 GitHub 用户名,内容原样)。不导也不影响新评论系统上线。

---

## 八、服务器部署设计

### 8.1 目录规划

```
/var/blog/
├── src/        # git 仓库工作区(Gitee 镜像)
├── server/     # Go 二进制 blog-api
├── web/dist/   # React 构建产物
├── blog.db     # SQLite 数据库
└── backups/    # 每日自动备份
```

### 8.2 Nginx 站点配置(`/etc/nginx/sites-available/blog`)

```nginx
# http 块内需先定义: limit_req_zone $binary_remote_addr zone=api:10m rate=10r/s;
#                     limit_req_zone $binary_remote_addr zone=admin:10m rate=1r/s;
server {
    listen 80;
    server_name _;

    root /var/blog/web/dist;
    index index.html;

    # 前端路由兜底(React Router history 模式)
    location / {
        try_files $uri $uri/ /index.html;
    }

    # 静态资源强缓存
    location /assets/ {
        expires 7d;
        add_header Cache-Control "public";
    }

    # 公开 API
    location /api/ {
        limit_req zone=api burst=20 nodelay;
        proxy_pass http://127.0.0.1:8080;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    # 管理 API(限流更严)
    location /api/admin/ {
        limit_req zone=admin burst=5 nodelay;
        proxy_pass http://127.0.0.1:8080;
        proxy_set_header X-Real-IP $remote_addr;
    }

    gzip on;
    gzip_types text/css application/javascript application/json image/svg+xml;
}
```

### 8.3 systemd 服务(`/etc/systemd/system/blog-api.service`)

```ini
[Unit]
Description=Blog API (Go/Gin)
After=network.target

[Service]
User=blog
WorkingDirectory=/var/blog/server
EnvironmentFile=/var/blog/.env          # 存 ADMIN_USER / ADMIN_PASS_HASH / JWT_SECRET
ExecStart=/var/blog/server/blog-api
Restart=always
RestartSec=3

[Install]
WantedBy=multi-user.target
```

### 8.4 发布脚本(`/var/blog/deploy.sh`)

```bash
#!/usr/bin/env bash
set -e
cd /var/blog/src
git pull gitee main                    # 从 Gitee 镜像拉,国内线路稳
cd server && go build -o /var/blog/server/blog-api ./cmd/server
cd ../web   && npm ci && npm run build
systemctl restart blog-api
echo "deploy done: $(date)"
```

每次发布流程: 本地 `git push`(GitHub + Gitee 双推)→ ssh 上服务器跑 `./deploy.sh`。稳定后可在 GitHub Actions 里加一步 SSH 自动执行,实现推送即上线(升级项,不阻塞上线)。

### 8.5 备份(cron,每日凌晨)

```bash
# crontab -e
0 4 * * * sqlite3 /var/blog/blog.db ".backup /var/blog/backups/blog-$(date +\%F).db" && find /var/blog/backups -mtime +14 -delete
```

P2 可选: 备份文件自动推送到 GitHub 私有仓库或对象存储,防单机故障。

---

## 九、安全设计(IP 直连、暂无 HTTPS 场景)

| 项 | 措施 | 优先级 |
|---|---|---|
| 防火墙 | `ufw` 仅放行 22/80;云控制台安全组同步收紧 | P0 |
| SSH | 密钥登录、禁用密码、禁 root 直登 | P0 |
| Go 服务 | 只监听 127.0.0.1,不暴露公网 | P0 |
| 管理后台 | bcrypt 强密码 + JWT 短有效期 + 登录失败限流(5 次锁 15 分钟) | P0 |
| 评论接口 | 频率限制 + 蜜罐 + 长度校验(见 4.3) | P1 |
| HTTPS | **裸 IP 无法签发 Let's Encrypt 证书**,暂走 HTTP;管理操作建议只在家用网络环境下进行;备案域名后立即上 443 + 证书 | P2(依赖域名) |
| 可选加固 | `/api/admin` 加 IP 白名单(家庭宽带 IP 变动时 ssh 改配置)或自建 WireGuard 内网访问 | P2 |

---

## 十、实施阶段计划

| 阶段 | 内容 | 优先级 | 预估 |
|---|---|---|---|
| 0 | 服务器初始化: 专用用户、ufw、SSH 加固、安装 Nginx/Go/Node/Git、Gitee 镜像仓库 | P0 | 0.5 天 |
| 1 | Go 后端骨架: 项目结构、SQLite 建表、posts/tags API、migrate 迁移脚本(29 篇入库) | P0 | 1~2 天 |
| 2 | React 前端迁移: 路由骨架、列表/详情/标签页、复用 Primer CSS、明暗主题 | P0 | 1~2 天 |
| 3 | 评论系统: 评论 API + 前端评论区组件 + 防垃圾限流 | P1 | 1 天 |
| 4 | 管理后台: JWT 登录、Markdown 文章编辑器、评论审核 | P1 | 1~2 天 |
| 5 | 部署上线: Nginx 配置、systemd、deploy.sh、备份 cron、**停用 GitHub Pages** | P1 | 0.5 天 |
| 6 | 收尾: RSS/sitemap 动态生成、FTS5 全文搜索、Giscus 评论导入、浏览量统计、(可选)Actions 自动部署 | P2 | 1 天 |

**建议节奏**: 阶段 0→2 做完后即可先让站点跑起来(文章可读),评论与管理后台迭代跟上——任何时候服务器上都有一个可用版本。

---

## 十一、风险与注意事项

1. **SEO 收录下降**: IP 直连 + 无 HTTPS,搜索引擎收录与权重会明显弱于 GitHub Pages。个人博客/技术笔记场景影响可接受;未来备案域名后可恢复,slug 保持不变以支持 301。
2. **备案提醒**: 国内云服务器绑定域名**必须 ICP 备案**,否则域名访问 80/443 会被服务商阻断。确定域名后尽早提交备案(通常 1~3 周)。
3. **旧链接失效**: 停用 GitHub Pages 后,`lird-e.github.io/Blog` 的历史收录会 404。如有外链(简历、他人引用)指向旧地址,可考虑在 Pages 仓库留一个跳转页过渡。
4. **Giscus 评论数据**: 不会自动迁移,不导出就丢弃(见 7.2)。
5. **单机无冗余**: 无高可用;每日备份 cron 兜底,P2 建议异地备份。
6. **服务器访问 GitHub 不稳**: 已用 Gitee 镜像规避;若双推麻烦,Gitee 支持"强制同步"按钮,或用 GitHub Actions 自动同步到 Gitee。
7. **本地代理依赖**: 你本地 `127.0.0.1:7897` 代理不稳定,`git push` 前需确认代理在线;服务器侧已与 GitHub 解耦,不受影响。
