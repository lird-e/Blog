# 博客项目优化审查报告（2026-09-25）

> 审查范围：`server/`（Go 后端全部 12 个文件）、`web/src/`（前端全部 20 个文件）、`deploy/`（部署配置）、git 仓库状态。
> 总评：重构后代码质量整体不错——分层清晰、防垃圾四件套齐全、systemd/Nginx/备份文档完善。但存在 **1 个必然复现的功能 Bug**、**2 个安全/数据风险**，以及若干 SEO 与细节优化点。

---

## P0 —— 高优先级（功能错误 / 数据风险，建议立即处理）

| # | 问题 | 位置 | 影响 |
|---|------|------|------|
| 1 | 后台「发布状态切换」按钮必然失败 | `web/src/pages/admin/AdminPosts.jsx:19-30` | 功能完全不可用 |
| 2 | `clientIP()` 无条件信任 `X-Real-IP` 头 | `server/internal/handlers/handlers.go:173-179` | 端口意外暴露时可绕过限流与登录锁定 |
| 3 | 重构后的全部新代码尚未提交 git | 仓库根（`server/`、`web/`、`deploy/` 均 untracked） | 只有一份本机拷贝；无法发布 |

**1. togglePublish 必然报「正文不能为空」**
列表接口 `db.ListPosts`（`server/internal/db/db.go:193-197`）的 SELECT 不包含 `content_md`，因此前端拿到的 `p.content_md` 是 `undefined`，`JSON.stringify` 会直接丢弃该字段（`AdminPosts.jsx:25`）。PUT 到后端后，`fillPost`（`server/internal/handlers/admin.go:134-136`）校验正文非空 → 永远返回 400。
**修复**：新增轻量接口 `PUT /api/admin/posts/:id/published` 只更新 published 字段（推荐），或前端切换前先 `GET /api/admin/posts/:id` 拿全文再提交。

**2. X-Real-IP 伪造可绕过防刷**
`main.go:42` 的 `SetTrustedProxies(nil)` 本意是不信任代理头，但 `clientIP()` 手工读取 `X-Real-IP` 绕过了这层保护。正常走 Nginx 时该头会被覆盖、无风险；可一旦 8080 端口因防火墙/安全组配置失误对外暴露，攻击者可每次请求伪造不同 IP → 评论限流（`handlers.go:127-134`）与登录失败锁定（`admin.go:46-50`）全部失效，可无限爆破密码。
**修复**：改为 `r.SetTrustedProxies([]string{"127.0.0.1"})` 并统一使用 `c.ClientIP()`（Gin 仅在连接来自可信代理时才解析头），删除手工读头的逻辑。

**3. 新代码未入库**
`git status` 显示 `server/`、`web/`、`deploy/`、`README.md`、重构方案文档全部为未跟踪状态，暂存区还挂着旧静态版的删除记录。所有重构成果只存在于桌面这一份；且 `deploy.sh` 依赖 `git pull gitee main`，不提交就无法部署上线。
**修复**：分批提交（建议 server / web / deploy+docs / 旧版删除 四个 commit），推 GitHub + Gitee 双远端。

---

## P1 —— 中优先级（SEO / 安全加固，建议近期安排）

| # | 问题 | 位置 | 影响 |
|---|------|------|------|
| 4 | SPA 无逐页标题/meta，SEO 倒退 | `web/index.html:16-18`、各页面 | 所有页面同标题同描述，无 OG 标签 |
| 5 | 全站 HTTP 明文，管理凭据可被窃听 | `deploy/README.md` 安全备忘 | 登录密码/JWT 明文传输 |
| 6 | 改 slug 后旧链接直接 404 | `server/internal/handlers/admin.go:204-210` | RSS/搜索引擎已收录链接失效 |

**4. SEO**：旧静态版每页 title 独立生成，重构后 `index.html` 固定 `<title>我的博客</title>`，前端从不更新 `document.title`。
- 最小改法：各页面 `useEffect` 中设置 `document.title = `${post.title} · 我的博客``，并同步 meta description（Home/Post/Tags/Search/About/NotFound）。
- 进阶改法：Go 端对 `/` 与 `/post/:slug` 直接返回注入了标题/描述/OG 标签的 index.html（Nginx 将这些路径反代给 Go），爬虫无需执行 JS。博客站内容被收录很重要，建议至少做最小改法。

**5. HTTPS**：域名备案通过后立即上 Let's Encrypt + 301（`SITE_URL` 同步改）；过渡期建议先启用 README 里已列出的 `/api/admin` IP 白名单（家用宽带 IP 会变，可配 DDNS）。

**6. slug 301**：posts 表加一列 `old_slug`（或独立 redirects 表），`GetBySlug` 未命中时查旧 slug 返回 301，保住已收录链接。

---

## P2 —— 低优先级（细节打磨，按需处理）

| # | 问题 | 位置 |
|---|------|------|
| 7 | sitemap 硬编码最多收录 100 篇（50×2 页），超出静默丢失；`_ = total` 写法别扭 | `server/internal/handlers/seo.go:83-97` |
| 8 | 浏览量每次访问 +1 无去重；返回值是自增前的旧值（首次显示 0） | `server/internal/handlers/handlers.go:57`、`db.go:252-254` |
| 9 | limiter 过期 key 只在被再次触碰时清理，永久驻留内存（量小影响可忽略，可加定时清扫） | `server/internal/limiter/limiter.go:26-31` |
| 10 | `SITE_URL` 以 `/` 结尾时 robots.txt 产生 `//sitemap.xml` | `seo.go:121` |
| 11 | Nginx：gzip_types 缺 `application/rss+xml`；`index.html` 未设 `no-cache`（发新版后老用户可能拿到旧 HTML 引用已删除的 hash 文件）；带 hash 的 Vite 产物可给 `immutable` 一年；缺 `nosniff`/`X-Frame-Options`/`Referrer-Policy` 安全头 | `deploy/nginx-blog.conf:18-21,47-48` |
| 12 | 搜索防抖后仍有慢响应晚到覆盖新结果的竞态，可用 AbortController 取消上一请求 | `web/src/pages/Search.jsx:12-23` |
| 13 | token 过期无统一 401 处理：编辑中途过期只报错、不引导重新登录 | `web/src/api.js:26-31` |
| 14 | 编辑器无未保存离开提醒（误点路由/关闭页面丢稿） | `web/src/pages/admin/PostEditor.jsx` |
| 15 | 管理端列表固定 `page_size=50` 无翻页，第 51 条起看不到 | `AdminPosts.jsx:13`、`AdminComments.jsx:20` |
| 16 | 小问题：`AdminPosts.jsx:3` 导入了未使用的 `fmtTime`；`Home.jsx:48` 用原生 `<a href="/tags">` 导致整页刷新，应改 `<Link>` | — |
| 17 | `deploy.sh` 用 `rm -rf dist && cp -r` 有瞬间 404 窗口，建议复制到临时目录后 `mv` 原子替换 | `deploy/deploy.sh:22-23` |
| 18 | 服务器每日备份只落在同一块盘，建议每周拉一份回本地或推对象存储 | `deploy/README.md` §6 |
| 19 | 无自动化验证：可加一个最简 CI（非部署）跑 `go vet ./... && go build ./... && npm run build` 防回归 | — |
| 20 | 本地杂物可清理：`web/vite.config.js.timestamp-*.mjs`、`data/blog-api.exe~`、`data/server.log`；`data/passhash.txt` 是敏感文件，勿截图外传 | — |
| 21 | 站点名「我的博客」硬编码在前端多处（`Layout.jsx:50,68`、`Home.jsx:29`、`Admin.jsx:81`），改站名要连前端一起改；可由后端配置接口下发 | — |
| 22 | 零测试覆盖：建议先给 `slugify` / `NormalizeTags` / `Excerpt` / `hashWithSalt` 补表驱动单测，成本低收益高 | — |

---

## 做得好的地方（保持）

- 防垃圾四件套（蜜罐 + 时间阈值 + 频率限制 + 加盐哈希）设计完整，评论/登录双限流独立实例
- JWT 校验限制了签名算法族（`auth.go:35`），bcrypt 存密码，IP/邮箱只存加盐哈希
- `fillPost` 统一校验入口、slug 冲突处理、迁移脚本幂等（UpsertPost 保 id/views）
- systemd 基础加固（NoNewPrivileges/ProtectSystem）、专用 blog 用户、防火墙文档齐全
- SQL 全部参数化，无注入风险；`dangerouslySetInnerHTML` 仅用于服务端渲染的作者内容

## 建议处理顺序

1. **先提交代码**（P0-3，5 分钟）→ 2. **修 togglePublish**（P0-1）→ 3. **修 X-Real-IP**（P0-2）→ 4. 页面标题（P1-4 最小改法）→ 5. 其余按需。

---

## 修复记录（2026-09-25 同日完成）

| 项 | 状态 | 说明 |
|---|---|---|
| P0-1 togglePublish | ✅ 已修 | 新增 `PUT /api/admin/posts/:id/published` 专用接口（`db.SetPostPublished` + `AdminSetPostPublished`），前端只传 published，正文不再有误伤风险 |
| P0-2 X-Real-IP | ✅ 已修 | `SetTrustedProxies([]string{"127.0.0.1"})`，全部改用 `c.ClientIP()`，删除手工读头逻辑 |
| P0-3 代码入库 | ✅ 已修 | 重构成果已分批提交（push 由作者执行） |
| P1-4 逐页 SEO | ✅ 已修 | 新增 `web/src/seo.js`，Home/Post/Tags/Search/About/NotFound 全部接入 title + meta description |
| P1-6 slug 301 | ✅ 已修 | 新增 redirects 表；改名自动记录、旧 slug 返回 `redirect_to`、前端无感替换地址；删文/建文时同步清理跳转记录 |
| P2 批量 | ✅ 已修 | sitemap 按总数全量翻页；浏览量同 IP 同文章每日 1 次且返回自增后值；limiter 机会式清扫过期 key；robots.txt 去重斜杠；搜索 AbortController 防竞态；api 层 401 统一登出广播；编辑器未保存守卫（beforeunload + 取消确认）；管理端文章/评论列表分页；移除未用 import、`<a>` 改 `<Link>`；Nginx 增加安全头 / index.html no-cache / hash 产物 immutable / gzip 补全；deploy.sh dist 原子替换；新增 CI（go vet/build/test + npm build）；新增 Go 单测（slugify/NormalizeTags/SplitTags/Render/Excerpt/hashWithSalt）；清理 vite 临时文件并加入 gitignore |
| P1-5 HTTPS | ⏳ 运维项 | 待域名备案后上 Let's Encrypt；过渡期建议启用 /api/admin IP 白名单 |
| P2-21 站点名硬编码 | ⏳ 未改 | 影响小，后续可由后端配置接口下发 |
| P2-18 备份异地化 | ⏳ 运维项 | 建议每周从服务器拉一份备份回本地 |

验证：`go vet ./...`、`go build ./...`、`go test ./...` 全部通过；`npm run build` 通过；本地冒烟测试（文章接口 404/重定向跳转）通过。

